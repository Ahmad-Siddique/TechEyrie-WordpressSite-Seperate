"use client";

import CustomersSection from "../icomat-home1/CustomerSection";

/** Image-based customer section for /premium-support1 (no videos). */
export default function CustomerSection(props) {
  return <CustomersSection pageKey="wp-premium-support1" {...props} />;
}
